[package]
name = "partisync-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PartiGraph 资产图谱：Entry/ContentIdentity/Tag DAG/Sidecar + SQLite/redb 仓储 + oplog"

[dependencies]
partisync-core = { path = "../partisync-core", version = "0.1.0" }
partisync-cas = { path = "../partisync-cas", version = "0.1.0" } # ADR-0002 同层裁定：内容身份需要哈希
partisync-provider = { path = "../partisync-provider", version = "0.1.0" } # ADR-0007 同层裁定：远端索引缝合点
sqlx = { workspace = true }
walkdir = { workspace = true }
notify = { workspace = true } # ADR-0005：文件事件源
blake3 = { workspace = true } # M2-WP03 merkle.rs 状态叶哈希
tokio = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
