//! PartiSync 命令行界面（骨架占位）。子命令按里程碑逐步落地：M0 本地引擎，
//! M1 协议与 serve，M2 sync。规格见 docs/specs/。

fn main() {
    println!(
        "partisync {}: skeleton (M-1 bootstrap)",
        env!("CARGO_PKG_VERSION")
    );
}
