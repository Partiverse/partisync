# SPEC: M6-WP01 —— 产品化演示包（CLI + MCP + 公开数据集一键 demo）

版本: 0.1 · **状态: 批准（2026-09-26 用户指令「M6:C」）** · 关联:
调研方案 §6.6（M6 product 化）、M4-WP03（MCP server 收官）、M5-WP04
（CLI event-drain 收官）、M5-WP03（CLI scan-plan 收官）
负责人: @lead · 起草日期: 2026-09-26

## 范围与目标

**M6 选 C**（最小风险 path）： 完整 CLI + MCP 双入口的本地/局域网
"演示包"——非商业 SaaS， 目标 = 投资人/招聘 demo 素材 + 用户上手 5
分钟跑通典型场景。

**任务分解**：
1. 公开数据集准备脚本（LCSTS / CMRC2018 一键拉+落盘， 国内
   `hf-mirror.com` 镜像； 选定 BGE 中文模型）
2. 一键 `scripts/demo.sh` —— 拉数据 + 启 hub + 索引 + demo 查询
3. 三段 5 分钟 demo 录制脚本（终端脚本）： 本地索引 / 联邦双节点 /
   LLM MCP 检索
4. README 改造 + Quick Start 段落

**M5 收官状态为输入**： 12 个 CLI 子命令已齐、 MCP server 在
`partisync-mcp`， 联邦 + 事件流 + 扫描均闭环。

## 验收

- [ ] 一键 demo： clone → `scripts/demo.sh` → 1 个 hub + 1 万 doc
      索引 + 5 个 demo 查询命中 < 5 min（macOS/Linux）
- [ ] MCP 入口可被 Claude Desktop / Cursor 通过 stdio 连接
- [ ] 三段 demo 录制脚本（终端命令 + 预期输出）
- [ ] README Quick Start 段 + 截图位
- [ ] 回归：fmt/clippy/test 全绿

## 涉及文件

docs/specs/M6-WP01.md（本文件）； scripts/demo.sh（新建）； README.md
（修订）； crates/partisync-ai/Cargo.toml（feature 门控， ADR-0018 已
设， 无新依赖）。
