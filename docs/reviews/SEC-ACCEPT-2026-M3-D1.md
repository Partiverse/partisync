# D1 密码学面审计验收与清偿登记（SEC-AUDIT-2026-M3-002 / 003）

> **登记编号**: SEC-ACCEPT-2026-M3-D1  
> **验收对象**: SEC-AUDIT-2026-M3-002（密码学面，Pass with Caveats）+ SEC-AUDIT-2026-M3-003（补充审计，Mixed）  
> **验收人**: @lead（AI 代理执行验收与整改，审计主体为独立会话 sess_d0c875fd / sess_4323309e，与实现会话无上下文交叉）  
> **验收日期**: 2026-09-20  
> **结论**: **D1（外部密码学审计，M2 遗留债务）实质清偿**——依据用户 2026-09-20 拍板的「AI 独立审计替代外部委托」口径（M3-WP00 债务节修订）

## 一、验收裁定

**SEC-AUDIT-2026-M3-002（密码学面）：通过（Pass with Caveats）。**
M2 checklist 24 子项逐项复核（22 项与 M2 审计一致、2 项为口径更新），M2 四项
缺陷整改（P1×2/P2×2）经独立二次确认全部真实落地；KDF 冻结声明成立；cargo audit
0 漏洞；6 项新发现全部为 P3 提示级。

**SEC-AUDIT-2026-M3-003（补充审计）：通过（整改后）。**
其 Mixed 结论中的 P0/P1 发现经核实为两类：① T03 实现进行中的工作树快照（审计
时点未提交/宏未导入/clippy 报错）——已在 T03 正式提交 `b85cb6c` 时全部解决
（clippy -D warnings 全绿、workspace 测试 0 失败、trailer 完整）；② deny 基线
失败——已按其建议以独立任务关闭（见下表）。

## 二、发现处置闭环

| 来源 | 级别 | 发现 | 处置 | 状态 |
|---|---|---|---|---|
| 003 §四 | P0 | hub 工作树事故（T03 未提交 + tests 不编 + clippy 报错） | T03 正式提交 `b85cb6c`（含全部 trailer），门禁全绿 | ✅ 关闭（时序：审计快照 → 当日完成） |
| 003 §六 | P1 | deny licenses/bans FAILED | **M3-WP00-T03**（`2671707`）：ADR-0012 + CDLA-Permissive-2.0 白名单 + 14 处 path 依赖显式版本根治 wildcard；licenses/bans/sources 全绿 | ✅ 关闭 |
| 003 §三 | P1 | P7 交接三要素（跨分片聚合 / 陈旧窗口 / 分裂双读）须在 WP03 规格显式回答 | 已登记为 WP03 规格起草的前置要求（M3-WP00 债务节 + 本文件 §三）；WP03-T00 规格卡执行 | 📌 移交 WP03（规格未起草，非本验收可关闭项） |
| 003 §五 | P2 | ADR-0011 否决 sled 依据失真（已发 1.0.0-alpha.124） | ADR-0011 修订 1 补事实注脚，不重启选型 | ✅ 关闭 |
| 003 §二.2.4 | P2 | 读时修复 256 行上限无依据 | M3-WP01 §4 补取值依据（LIST 页深×页大小）+ T05/T06 复核条款 | ✅ 关闭 |
| 002 §四.1 | P3 | `pairing_session.shared_secret` 明文落盘（独立发现） | 登记为 WP03 决策点（保留 vs 加密落，随 D2 接线一并裁定） | 📌 移交 WP03 |
| 002 §四.3/4/5/6 | P3 | getrandom 三版本 / Mnemonic 时序 / KDF context 术语 / 测试种子 | 记录在案，随 WP03 局部密码学复核一并处理 | 📌 低优登记 |
| 002 §三.3 | P3 | D2 生产接线须局部密码学复核 | 已是 D2 清偿的显式前提（见 §四） | 📌 绑定 WP03 |

## 三、P7 不变量交接（WP03 规格前置要求，承接 003 §三.4）

M3-WP03 规格起草（G0 前）必须显式回答：
1. Merkle 树根跨 256 哈希分片 + 动态 range 分区的**聚合策略**
   （每分片子树 + 元根，或单棵跨平面虚拟根）；
2. entry↔children 跨平面投影陈旧窗口是否纳入「可观察状态」
   （M2-WP03 快路径声音性条件在 hub 拓扑下的重述）；
3. range 分裂「双读视图」期间的 Merkle 行走策略（须与 WP01 §3 分裂协议交叉引用）。

## 四、D1 清偿裁定与保留条件

**裁定：D1 实质清偿**（双层独立审计闭环：SEC-AUDIT-2026-M2-001 友邻审计 +
SEC-AUDIT-2026-M3-002/003 独立复核，整改全部闭环，治理链完整可追溯）。

**保留条件（不满足任一项时 D1 重新打开）**：
1. **D2 接线时**（M3-WP03 kdf_salt 生产全量接线）：由独立 AI 审计执行局部
   密码学复核（盐落库时机 / 跨空间一致性 / 撤销语义 / shared_secret 处置决策）；
2. **D3 接入时**（iroh 真实网络层，M2 遗留原样保留）：握手与传输面密码学
   影响须专项审计，002 建议该项考虑外部委托；
3. M3 关门报告按 WP00 要求显式登记本清偿口径与保留条件。

**KDF 冻结**：审计通过后按 M2 报告 §4 口径解冻日常演进；`blake3::derive_key`
标准化路径（SEC-AUDIT-2026-M2-001 P1-1 整改）为既定事实，任何 KDF 变更仍走
独立 PR + 双人评审（铁律 7）。

## 五、诚实披露

- SEC-AUDIT-2026-M3-001（开局治理面审计）的验收时序披露（签核先于审计报告
  回传）仍适用于本登记：D1 清偿裁定先于 003 报告的建议读到——但 003 的三项
  关闭条件（工作树 / deny / P7 论证）中前两项已在其后独立完成并验证，第三项
  显式移交 WP03 而非静默跳过；
- advisories 检查在本验收环境因 GitHub 不可达未直接执行；002/003 审计环境
  已执行（0 漏洞 / 406 deps），网络恢复后本仓库补跑留痕；
- 两份审计报告由审计会话直接写盘（未过 git），随本登记一并首次入库。
