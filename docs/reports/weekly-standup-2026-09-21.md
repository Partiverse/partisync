# 周站会摘要（2026-09-15 ~ 2026-09-21）

> 统计口径：`git log --since="7 days ago"`，全部 127 次提交集中在 09-18 ~ 09-20，共 +31,331 / -1,367 行。

**一句话**：一周内从 M0 基础推进到 **M2 正式关门、M3 顺利开局**，当前 M3-WP02 复制层已完成 T05，T06/T07 进行中。

## 里程碑主线

### ① M0/M1 收尾（09-18 周五，54 commits）

- M0 工作区骨架全量落地（core/cas/graph/cli/xtask 等 7 个 WP）。
- M1 性能收官：目录排序复合索引 **49.4ms，提升 11×**（`eed0f37`）；index-remote 作业化 + 令牌桶限速实测 1M/s（`03a66dd`）。

### ② M2 关门（09-19 周六，55 commits）— 本周最大块

- 9 个 WP 全部完成：同步核（oplog/回环防护）、Merkle 对账（分级桶+水位快路径）、双向同步（冲突血缘/Tag LWW）、占位符骨架、版本回收（trash/GC/pin）、iroh 设备配对（Ed25519+X25519+ECDH+助记词）、块差分传输、E2EE（Argon2+HKDF-blake3+XChaCha20-Poly1305）、混沌测试床（7 项故障矩阵）。
- 密码学审计整改闭环：P1/P2 全部清偿（KDF 标准化、ephemeral_sk 移出持久层等，`4d60e29` / `2b9a805`）。
- 关门 KPI：**10⁵ 差异同步 14.1s（预算 5min，达标）**，关门报告入库（`87dbca4` / `8d561cb`）。

### ③ M3 开局（09-20 周日，18 commits）

- WP00 总纲 + 开局 AI 独立审计、D1 密码学面审计验收（`ae1c9ca`）。
- **WP01 分片元数据 7 任务全部完成**：fjall 3.1 底座（ADR-0011）、blake3 哈希平面、动态分裂协议、rename O(1)+读时修复、**10⁷ 导入基准 KPI 达标**、交互演示 UI。
- **WP02 复制层 T01-T05 已提交**：规格+ADR-0013、openraft 0.9.25 进场、raft 存储→fjall 适配（含官方一致性套件）、TCP 网络层、组生命周期（**failover<10s、leader 转移<5s 实测达标**，`57f76fc`）。

## 已合并 PR

仅 1 个：**#1「CI 首跑验证（五道门禁）」**（Partiverse，09-19 合并）。其余工作按仓库惯例直接提交 main，均带 Task-ID 追溯。

## 当前状态 / 下一步

- 工作区有未提交变更（`replica.rs` 修改 + `benches/wp02.rs` 新增），对应 **WP02 收尾：T06 钉子文档（双人全审）+ T07 基准**。
- M2 仅剩 G3 外部审计挂账（临时放行已登记）。
